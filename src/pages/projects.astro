---
import { getCollection } from "astro:content";
import CardList from '../layouts/CardListLayout.astro';
const allProjects = await getCollection("projects");
const title = "/projects";
---
<CardList title={title} collection={allProjects} />
