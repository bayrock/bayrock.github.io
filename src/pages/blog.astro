---
import { getCollection } from "astro:content";
import CardList from '../layouts/CardListLayout.astro';
const allPosts = await getCollection("blog");
const title = "/blog";
---
<CardList title={title} collection={allPosts} />
