---
import { getCollection } from "astro:content";
import CardList from '../layouts/CardListLayout.astro';
const allPosts = await getCollection("blog");
const pageTitle = "/blog";
---
<CardList pageTitle={pageTitle} collection={allPosts} />
