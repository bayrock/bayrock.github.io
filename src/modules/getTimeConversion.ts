export function millisecondsToSeconds(ms: number) { return ms / 1000; } 
export function secondsToMilliseconds(s: number) { return s * 1000; }
export function secondsToHours(s: number) { return s / 3600; }
export function minutesToHours(minutes: number): number { return minutes / 60; }
